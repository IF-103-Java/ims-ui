import { ItemSortableDirective } from './item-sortable.directive';

describe('ItemSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemSortableDirective();
    expect(directive).toBeTruthy();
  });
});
